<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<ul>
	<% @user.collections.each do |collection| %>
		<li><%= link_to collection.name, collection_path(collection.id) %></li>
	<% end %>
</ul>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Users', users_path %>
