<div class="collection_field">
	<%= f.label :name, "Album Name" %>
	<%= f.text_field :name %><br />
	<br />
	<%= f.fields_for :photos do |f1| %>
	  <%= render 'photo_fields', f: f1 %>
	<% end %>
	<p><%= link_to_add_fields "Add More Photo", f, :photos %></p>
</div>
