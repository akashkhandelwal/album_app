<h1>Listing Albums</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Action</th>
    <th></th>
    <th></th>
  </tr>

<% @collections.each do |collection| %>
  <tr>
    <td><%= collection.name %></td>
    <td><%= link_to 'Show', collection, class: 'btn btn-info' %></td>
  </tr>
<% end %>
</table>